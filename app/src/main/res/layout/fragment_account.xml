<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout style="@style/fragmentGeneric"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:gravity="fill"
    >

    <TextView
        android:id="@+id/welcome_text"
        style="@style/accountText"
        />

    <Button
        android:id="@+id/isa_button"
        style="@style/accountButton"
        android:text="@string/stocks_button"
        />

    <Button
        android:id="@+id/gia_button"
        style="@style/accountButton"
        android:text="@string/general_button"
        android:layout_below="@+id/isa_button"
        android:layout_marginTop="@dimen/progress_bar_size"/>

</RelativeLayout>